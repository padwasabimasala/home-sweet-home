#!/bin/sh
#curl -d "grant_type=password" -d "username=bob" -d "password=secret" 'https://abc123:ssh-secret@oc-eve-prod.herokuapp.com/oauth/token'
curl -d "grant_type=password" -d "username=adam.tanner@octanner.com" -d "password=iloveapples" 'https://abc123:ssh-secret@api.octanner.com/oauth/token'
